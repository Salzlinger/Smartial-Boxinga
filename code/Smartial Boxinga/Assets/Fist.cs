using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Fist : MonoBehaviour
{
    /*
    public GameObject Target1;
    public GameObject Target2;
    public GameObject Target1Prefab;
    public GameObject Target2Prefab;

    bool[] punchChecker = new bool[] { false, false };

    private void OnTriggerEnter(Collider other)
    {
        Destroy(other.gameObject);
        Debug.Log("Destroyed!!!");
        if (other.gameObject == Target1)
        {
            Debug.Log("this is Target1");
            punchChecker[0] = true;
            if(punchChecker[0])
            {
                Debug.Log("is drinn!");
             }

        } else if (other.gameObject == Target2)
        {
            Debug.Log("this is Target1");
            punchChecker[1] = true;
            if (punchChecker[1])
            {
                Debug.Log("is drinn!");
            }
        }
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (punchChecker[0] && punchChecker[1])
        {
            Instantiate(Target1Prefab, new Vector3(0, 2.65f, -5.27f), Quaternion.identity);
            Instantiate(Target2Prefab, new Vector3(1.83f, 2.65f, -5.27f), Quaternion.identity);
        }
        
    }
    */
}
